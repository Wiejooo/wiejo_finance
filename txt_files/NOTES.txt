Private app for finances

Notes:
sqlite3 finance.db - interaktyny tryb 
.tables - pokazuje wszystkie tabele
PRAGMA table_info(<nazwa tabeli>); - nazwy kolumn i typy
DROP TABLE IF EXISTS <nazwa tabeli>; - usunięcie tabeli
SELECT * FROM <nazwa tabeli> - pokazuje obiekty

pip freeze > requirements.txt aktualizacja wymagań 